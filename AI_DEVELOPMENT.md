# AI 开发协作记录

本仓库的 4.0 迁移由用户批准总体技术方向后，使用多个智能体线程规划，再由本实施线程检查现状、编写测试、修改代码并运行验证。

## 开发期 AI

开发期 AI 用于：

- 把 `game.js` 的职责拆分为 Vue、编辑器服务、共享领域模型和 `GameEngine` 适配器。
- 根据批准文档实现 Vue 3 + Vite + TypeScript strict + Phaser 3。
- 编写并执行 Vitest、Vue Test Utils、Node test、Playwright、安全、内容、性能和离线包检查。
- 生成受限病例蓝图编译器、AI 导演输入/输出白名单与本地确定性回退。
- 整理验收入口、已知限制和复现命令。

它没有替代最终人工通关、医学内容负责人签字、公开部署、视频录制或评委结论。无法导出的私有聊天原文标记为“未归档”，不会补写或伪造。

## 运行时 AI

运行时 AI 与开发期协作完全分开：

- 浏览器不保存或发送上游 API Key，只调用同源 `/api/director` 与 `/api/generate-case`。
- 服务端只接受有限 schema、事件白名单、节点白名单和有界文本。
- AI 只输出病例蓝图或危机计划，TypeScript 编译器/校验器决定能否进入运行时。
- 超时、HTTP 错误、未知字段、非法节点或非法文本均转入 `LocalCaseDirector`，UI 明确显示“本地导演”。
- 本地导演不是在线 AI；报告和演示不得混称。

## 关键提交

- `171c5a6`：移除浏览器 AI 密钥与上游直连，补安全测试。
- `318bb6e`：Vue 病例设计器、CQ2、严格蓝图校验。
- `158df0e`：编辑器草稿进入真实试玩链路。
- `17f2d20`：Phaser 病例运行时、AI 导演、六章内容。
- `303df1f`：首次引导、每日病例、进度、双人实例和结算报告。

最终候选、测试总数和发布状态以 `docs/evidence/TEST_REPORT.md` 在冻结 SHA 上的生成结果为准。
