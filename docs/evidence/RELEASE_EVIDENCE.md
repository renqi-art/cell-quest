# 发布证据状态

当前结论：`RELEASE BLOCKED`

自动化候选本身全部通过，但仍存在两类发布阻塞。

代码边界：

- Phaser 已承载六章病例、每日病例、AI 危机、双人职责和角色交换，但经典模式仍由 `LegacyGameEngineAdapter` 运行。
- 经典模式中的敌人、尖刺、弹簧、道具、子弹、冲刺、动态双人相机和完整音频尚未做到 Phaser 等价迁移。
- 因此当前实现是“病例模式 Phaser 完整链路 + 经典模式兼容层”，不是整个经典游戏引擎的最终 Phaser 替换；A11 全量手感与碰撞验收仍为 `BLOCKED`。

外部/人工证据：

- 正式在线 URL、部署 SHA 与线上健康检查。
- 六章逐章真实手工通关报告。
- 双人角色交换与最终报告。
- 实体参考机 1% low FPS 和手感签字。
- 离线包最终 hash 与干净机器启动记录。
- 90 秒视频、备份截图和连续三次小于 4 分 30 秒彩排。

解除条件：冻结候选 SHA，在隔离目录运行全部强制命令，完成 Gate A 人工项目，再完成 Gate B 在线、离线、证据与彩排项目。实现线程不得自行把本文件改为 `RELEASE APPROVED`。
